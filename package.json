{
  "name": "org-fixtrading-fix",
  "private": true,
  "description": "FIX (Financial Information eXchange) tag=value/SOH wire codec in portable .cljc",
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljk"
  }
}
